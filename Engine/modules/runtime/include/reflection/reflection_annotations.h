#pragma once

// These macros are intentionally empty for C++ compilation. The build-time
// reflection generator scans them in component headers and emits registration
// code for the editor/runtime reflection registry.
#define CYBER_REFLECT_COMPONENT(...)
#define CYBER_PROPERTY(...)
#define CYBER_FUNCTION(...)
